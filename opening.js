const age = 20;
const name = "Jan";

function welcome(age, name) {
    console.log(
        `Witam serdecznie na przykładowej stronie, nazywam się ${name} i mam ${age} lat.`
    );  
}

welcome(age, name);